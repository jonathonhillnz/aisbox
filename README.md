# aienv

`aienv` runs AI coding agents inside isolated Docker environments.

## Development

```bash
python -m venv .venv
. .venv/bin/activate
pip install -e ".[dev]"
pytest
```

## Install From This Repository

```bash
pipx install .
```
